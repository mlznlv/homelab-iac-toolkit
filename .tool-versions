# Supported versions of the language runtimes and standalone command-line tools
# used to develop and validate this repository.
#
# This file is the authoritative declaration for those tools. Python packages
# are declared in requirements-dev.txt. See docs/toolchain.md for how to read,
# obtain, and change these versions.
#
# Runtimes are declared at their supported series because the patch release is
# supplied by the platform that provides them. Standalone tools are pinned to an
# exact release.
#
# The format is the conventional `.tool-versions` layout: one `<tool> <version>`
# pair per line. It can be read directly, parsed by a script, or consumed by a
# compatible version manager.

# Language runtimes
python 3.13
node 24

# Project toolchain
opentofu 1.12.6
task 3.53.1
sops 3.13.3
age 1.3.2

# Repository validation
markdownlint-cli2 0.19.0
actionlint 1.7.12
gitleaks 8.30.1
lychee 0.24.2
zizmor 1.29.0
