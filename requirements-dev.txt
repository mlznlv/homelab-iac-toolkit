# Supported versions of the Python packages used to develop and validate this
# repository.
#
# This file is the authoritative declaration for the Python package dependency
# class. Language runtimes and standalone command-line tools are declared in
# .tool-versions. See docs/toolchain.md for how to read, obtain, and change
# these versions.
#
# This file is the declaration. Installs use requirements-dev.lock, which is
# generated from it and carries hashes, so that what is installed is verified:
#
#     python -m pip install --require-hashes --requirement requirements-dev.lock
#
# After changing a version here, regenerate the lock with the command in its
# header. scripts/check-python-lock.sh asserts the two still agree.
#
# ansible-core provides the Ansible engine only, and ansible-lint checks the
# Ansible content in this repository. Collection dependencies are declared by
# the milestone that introduces content needing them.

ansible-core==2.21.3
ansible-lint==26.8.0
check-jsonschema==0.36.1
yamllint==1.38.0
